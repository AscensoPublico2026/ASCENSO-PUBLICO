# Dependencias de los scripts de scripts/
# Instala con:  pip install -r requirements.txt

# Para scrape_simo.py (navegador headless que renderiza SIMO)
playwright>=1.40

# Para optimizar_imagenes.py (redimensionar y comprimir fotos)
Pillow>=10.0
